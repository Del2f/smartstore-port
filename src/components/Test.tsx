import React from "react";
import styled from "styled-components";

type Props = {

  };

function Test(props: Props) {
    return(
        <div>저는 테스트페이지 입니다.</div>
    )
}

export default Test;